<odoo>
    <record id="view_move_form_inherit_sale_button" model="ir.ui.view">
        <field name="name">account.move.form.inherit.sale.button</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>
        <field name="arch" type="xml">

            <xpath expr="//header" position="inside">
                <button name="button_create_sale_order"
                        string="Create Sale Order"
                        type="object"
                        class="btn-primary"
                        invisible="sale_order_id"
                />
            </xpath>

            <xpath expr="//field[@name='invoice_date']" position="after">
                <field name="sale_order_id" readonly="1" force_save = "1"/>
            </xpath>
        </field>
    </record>
</odoo>
