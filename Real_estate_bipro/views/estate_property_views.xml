<?xml version="1.0" encoding="UTF-8"?>

<odoo>
    <!-- List View -->
    <record id="estate_property_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Properties"  delete="false" decoration-success="state == 'offer_accepted'">
                <field name="name" width="100px" readonly="1"/>
                <field name="postcode" width="100px" readonly="1"/>
                <field name="bedrooms" width="100px" readonly="1"/>
                <field name="living_area" width="100px" readonly="1"/>
                <field name="expected_price" width="100px" readonly="1"/>
                <field name="selling_price" width="100px" readonly="1"/>
                <field name="date_availability" width="200px" optional="hide" readonly="1"/>
                <field name="property_type_id" width="200px" readonly="1"/>
                <field name="user_id" width="200px" readonly="1"/>
                <field name="buyer" width="200px" readonly="1"/>
                <field name="tag_ids" widget="many2many_tags" options="{'no_create': True, 'no_edit': False}"/>
            </list>
        </field>
    </record>

    <!-- form view, I will have to fix this later-->
    <record id="estate_property_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="action_mark_sold"
                        string="Sold"
                        type="object"
                        class="oe_highlight"
                        invisible="not show_mark_sold_button"/>

                <button name="action_cancel"
                     string="Cancel"
                        groups="Real_estate_bipro.real_button_security"
                     type="object" invisible="status in ['canceled','sold']"/>
                </header>


                <header>
                    <field name="state" widget="statusbar" statusbar_visible="new, offer_received, offer_accepted, sold"/>
                </header>

                <sheet>
                    <group>
                        <field name="name" nolabel="1" class="oe_title" style="font-size:36px;"/>

                    </group>
                    <group>

                        <group>
                            <field name="tag_ids" widget="many2many_tags" nolabel="1" options="{'color_field': 'color'}"/>
                            <field name="status"/>
                            <field name="property_type_id" options="{'no_create': True, 'no_edit': True}"/>
                            <field name="postcode"/>
                            <field name="date_availability"/>
                        </group>
                        <group>
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                            <field name="invoice_created" readonly="1"/>

                        </group>
                    </group>

                    <notebook>
                        <page string="Description">
                            <group>
                                <field name="description"/>
                                <field name="bedrooms"/>
                                <field name="living_area"/>
                                <field name="facades"/>
                                <field name="garage"/>
                                <field name="garden" />
                                <field name="garden_area" invisible=" not garden"/>
                                <field name="garden_orientation" invisible=" not garden"/>
                                <field name="total_area" invisible=" not garden"/>

                            </group>
                        </page>
                        <page string="Offers">
                            <field name="offer_ids" readonly="state in ['offer_accepted', 'sold', 'cancelled']"/>

                        </page>
                        <page string="Other Info">
                            <group>
                                <field name="user_id" />
                                <field name="buyer"/>
                            </group>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Search  View -->
    <record id="estate_property_search" model="ir.ui.view">
        <field name="name">estate.property.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" />
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>
                <field name="tag_ids"/>
                <field name="user_id" />
                <field name="buyer"/>
            </search>
        </field>
    </record>
    <!-- Action -->
    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
    </record>


</odoo>