<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_users_form_inherit_real_estate" model="ir.ui.view">
        <field name="name">res.users.form.estate.properties</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form"/>
        <field name="arch" type="xml">
            <!-- Add a new notebook page at the end -->
            <xpath expr="//notebook" position="inside">
                <page string="Current Real State Properties by Bipro">
                    <field name="property_ids">
                        <list editable="bottom">
                            <field name="name"/>
                            <field name="postcode"/>

                            <field name="bedrooms"/>
                            <field name="living_area"/>
                            <field name="expected_price"/>
                            <field name="selling_price"/>
                            <field name="tag_ids" widget="many2many_tags"/>

                        </list>
                    </field>
                </page>
            </xpath>
        </field>
    </record>
</odoo>
